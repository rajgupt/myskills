{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "SpecContract.schema.json",
  "title": "SpecContract payload",
  "description": "Payload schema for the `payload` field of a SpecContract envelope. STUB — fill in `properties` and `required` when you plan this skill. The envelope itself is validated by contract_envelope.schema.json.",
  "type": "object",
  "x-emitted-by": "S0",
  "x-consumed-by": ["S1","S2","S4","S5"],
  "properties": {},
  "required": []
}

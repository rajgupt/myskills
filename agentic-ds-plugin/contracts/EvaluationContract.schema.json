{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "EvaluationContract.schema.json",
  "title": "EvaluationContract payload",
  "description": "Payload schema for the `payload` field of a EvaluationContract envelope. STUB — fill in `properties` and `required` when you plan this skill. The envelope itself is validated by contract_envelope.schema.json.",
  "type": "object",
  "x-emitted-by": "S5",
  "x-consumed-by": ["S6"],
  "properties": {},
  "required": []
}

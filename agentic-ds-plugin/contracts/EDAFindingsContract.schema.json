{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "EDAFindingsContract.schema.json",
  "title": "EDAFindingsContract payload",
  "description": "Payload schema for the `payload` field of a EDAFindingsContract envelope. STUB — fill in `properties` and `required` when you plan this skill. The envelope itself is validated by contract_envelope.schema.json.",
  "type": "object",
  "x-emitted-by": "S2",
  "x-consumed-by": ["S3"],
  "properties": {},
  "required": []
}

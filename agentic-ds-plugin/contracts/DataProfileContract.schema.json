{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "DataProfileContract.schema.json",
  "title": "DataProfileContract payload",
  "description": "Payload schema for the `payload` field of a DataProfileContract envelope. STUB — fill in `properties` and `required` when you plan this skill. The envelope itself is validated by contract_envelope.schema.json.",
  "type": "object",
  "x-emitted-by": "S1",
  "x-consumed-by": ["S2"],
  "properties": {},
  "required": []
}
